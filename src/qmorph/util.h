#pragma once

#ifndef _UTIL
#define _UTIL
#include <initializer_list>
#include <glm.hpp>

float* solveKMEquation(glm::vec3 AC, glm::vec3 AD, glm::vec3 AB);
//Point convertLaplacianOrigin(Point origin);
//
//Point bisectorOfCpoints(Point left, Point right, Point normal);

#endif